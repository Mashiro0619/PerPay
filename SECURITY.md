<!-- SPDX-License-Identifier: Apache-2.0 -->

# 安全策略

## 报告漏洞

请使用 GitHub Security Advisory 的私密报告功能提交漏洞。不要创建公开 Issue，也不要附带真实密钥、完整账务记录、回调地址或可识别个人信息；请使用最小化、脱敏的复现材料。

报告应尽量包含受影响版本、影响范围、复现条件和建议缓解措施。维护者会先确认收到，再完成影响评估、修复和协调披露。

## 支持范围

项目尚未发布稳定版本，当前代码不能用于处理真实资金。稳定发布后，本文件将列出受支持版本和安全补丁期限。

## 安全边界

- 不接受降低公网入口必须使用 TLS 的要求、绕过响应验签、记录 secret 或放宽出站网络限制的变更。
- 示例数据必须为人工生成且明确标记为 fake。
- 依赖漏洞、密钥泄露和账务一致性问题按安全事件处理。
- 未经维护者协调，请勿对第三方平台或他人账户进行测试。

## 浏览器界面

公开收银台 token 是高熵能力地址，只用于读取单笔订单的最小付款投影；页面和 JSON 响应均使用 `no-store`，不加载第三方资源，也不把内部订单 ID、商户 API 密钥或经营码原始内容写入 HTML。二维码由服务端从订单创建时冻结并绑定的经营码版本本地生成，并继续受付款入口就绪门禁和公开限流保护。浏览器倒计时只用于展示，不能决定订单是否成功、关闭或过期。

管理后台不接触商户 HMAC 密钥。所有写请求要求同源 Cookie 与双提交 CSRF；资金、冲突和通知补发操作还要求近期密码验证。step-up 会轮换会话和 CSRF，前端必须使用新 token 重放同一个用户意图及其原操作 UUID，不能生成第二个操作编号。界面使用仅允许同源脚本、样式、连接、图片和字体的 CSP，不允许内联脚本、内联样式、`eval`、外部 CDN 或被第三方页面嵌入。

## 平台接入与出站网络

应用 RSA 私钥与 API 密钥同属高敏感凭据。填写后的 `docker-compose.yml`、容器环境、诊断包和日志不得公开；文件访问权限、主机管理员权限和备份访问权限都应按生产密钥管理。Linux 部署目录权限不得宽于 `0700`，Compose 文件不得宽于 `0600`。Docker daemon 管理员仍可读取容器环境，因此不得在不可信的共享 Docker 主机部署。平台公钥必须从可信控制台或文档渠道取得，不能用应用公钥替代，也不能通过不可信消息临时更新。API 密钥轮换会永久退役旧指纹，不能通过配置回滚重新启用；若误填新密钥，应继续生成另一份新密钥，而不是恢复旧值。

账务适配器只允许连接预置的生产或沙箱 HTTPS 网关和 443 端口，不跟随重定向。每次请求先解析网关域名，拒绝私有、环回、链路本地、文档保留和其他非公网地址，再把选定地址绑定到实际 TLS 连接，同时保留原始主机名用于 SNI 和证书校验。任何新增网关、代理、重定向或自定义证书信任都属于安全边界变更，需要独立威胁评估。

入站反向代理默认不受信，应用忽略其转发来源头。启用真实客户端来源解析时，代理必须覆盖 `X-Forwarded-For`，配置只能信任应用直连看到的代理 IP/CIDR，并维持应用端口不可从公网直达。应用从右向左剥离受信跳点，拒绝畸形或过长链。信任全部地址、信任客户端网段，或在未验证 Docker/CDN 实际连接拓扑时猜测代理地址，都会破坏管理员限速隔离和审计证据。

成功响应只有在使用配置的平台公钥完成 V3 RSA-SHA256 验签后才能进入规范化账务。HTTP 错误响应可以作为诊断证据保存，其验签状态会明确区分成功、失败和未执行；未验签或验签失败的内容不会进入规范化账务。响应体、响应头和诊断字段都有硬限制，超限响应会使扫描失败，而不会无限占用内存或数据库。

账务采集和自动确认任务都必须先有一次成功记录，且最近成功时间不得超过 `PERPAY_ALIPAY_MAX_SUCCESS_AGE_SECONDS`，`/readyz` 才能返回 HTTP 200。任一任务从未成功、已经停止或成功记录过期时，系统拒绝创建新订单，并且仍可付款的公开收银台不会返回付款指令；已经确认、关闭或过期的收银台仍可查询状态。既有成功仍在新鲜度窗口内时，后续瞬时故障只使状态降为 `degraded`；过期后重新返回 503。运维监控必须同时检查采集与自动确认的 `last_success_at`、年龄和 `consecutive_failures`，不能把 HTTP 200 解释为两个任务当前都正常。

## 订单通知与接收端

通知允许 origin、每笔订单的完整目标 URL 和 HMAC 密钥都属于安全配置。通知只接受一个 HTTPS DNS origin；每次尝试独立解析 DNS，要求全部地址均为公网地址，将结果固定到 TLS 连接并复核远端地址。实现不读取代理环境变量、不跟随重定向、不允许协议升级或压缩响应。放宽 IP 范围、允许混合公网/私网结果、增加自定义 CA 或代理都属于 SSRF 与信任边界变更，必须独立评估。

通知密钥必须独立于管理员密码和 API 密钥。数据库只保存版本、公开 key ID 和密钥指纹，不保存通知明文密钥；填写后的 Compose、容器环境和接收端配置仍必须按最高敏感级别保护。密钥轮换立即影响新尝试，已经退役的指纹不能回滚启用。

通知采用可能重复的有限重试，不保证最终送达或恰好一次；永久失败或达到尝试上限后会进入死信。接收方必须对原始 body 验证 HMAC，并按 `event_id` 持久化幂等结果后再返回严格 ACK；不能把 delivery ID、attempt、网络连接成功或普通 HTTP 200 当作业务事件只发生一次的证明。事件 body、完整目标 URL、连接诊断和人工补发理由可能包含业务或个人信息，不能写入公开日志和问题单。详细规则见 [通知接收契约](docs/WEBHOOKS.md)。

## 账务证据与一致性

已验签的原始页、原始明细、采集错误和冲突记录包含账户标识、交易备注、对手信息和完整响应，可能构成个人信息与财务数据。它们与 SQLite 数据库、WAL 文件和备份具有相同的最高敏感级别；排障材料必须最小化并脱敏。

原始证据与规范化账务事实在数据库中受到不可变触发器、外键、唯一约束和完整性检查保护。相同外部流水号出现不同事实、同一时间片请求返回不同原始响应或字段无法安全规范化时，系统会隔离冲突而不是覆盖。不要绕过应用直接修改这些表；人工修复必须先保全数据库和原始证据，并通过可审计的专用流程完成。

审计链检查点和证据 SHA-256 都与业务数据保存在同一个 SQLite 数据库中。它们用于发现损坏、部分写入和未同步修改，但不是数据库外的真实性证明，也不能对抗已经拥有数据卷 DDL/写权限并能协调重算检查值的攻击者。需要独立防篡改证据的部署必须把签名检查点或追加式审计记录发送到由另一信任域控制的外部系统；当前单机版本不提供该能力。

采集结果首先只形成未分配账务条目。金额、经营码配置和时间区间只能生成推断候选；当账户、币种、精确金额、不可变槽位代次、时间区间和当前候选集合在同一写事务中仍然严格唯一，且没有证据冲突或既有认领时，系统自动写入 `CONFIRMED / INFERRED`、平衡分录、不可变订单事件和 Outbox。任一条件不满足都不得自动确认，只能进入可审计的异常处理或由管理员显式认领。`INFERRED` 是部署者接受的经营码金额推断，不是平台签发的订单付款证明；调用方必须以已提交的订单状态和事件为准，不能只凭同额流水、候选数量或扫描成功判断已支付。

## 更新与镜像

正式部署必须使用发布说明给出的固定版本镜像和 OCI digest，不使用 `latest`，也不接受来源不明的重打包镜像。镜像更新只由宿主上的 Docker Compose 执行；长期应用容器不得挂载 Docker Socket、调用 Docker CLI、修改自身镜像引用或静默重启自身。自动替换容器的第三方工具会绕过迁移说明、升级确认和恢复检查，不属于受支持的资金系统更新路径。

数据库、WAL/SHM 和迁移前恢复点位于应用数据卷；周期备份、备份状态和备份锁位于独立备份卷。`backup` 服务使用 SQLite online backup 创建自包含副本，复核 SHA-256、schema、实例身份和数据库完整性后再执行数量保留策略。Compose 重建容器不会删除两个卷，但 `docker compose down --volumes`、手工删卷和宿主磁盘故障仍可能同时破坏主库与同机备份；迁移前恢复点也只能用于版本回退。填写后的 Compose 包含平台私钥和服务密钥，必须加密异地保存并按最高敏感级别保护。

新镜像完成配置校验、数据库迁移并使 `/healthz` 返回 HTTP 200 后，才算应用启动成功。`/readyz` 只表示账务采集与自动确认当前可收款；平台关闭、首次扫描尚未成功或成功记录过期时可以返回 503，但这不应阻止容器存活或备份运行。若新 schema 不再被旧镜像支持，直接把镜像标签改回旧版本不是数据库回滚。恢复迁移前快照会让新版本启动后的业务写入从活动库中消失，因此它不是无损回滚：必须先停止应用和外部入口，保留被替换的新库，并明确接受数据回退或先制定核对、合并和前向修复方案。维护命令会完整保留被替换数据库，但不会自动合并分叉数据，也不能抵御已经获得数据卷写权限的恶意主机管理员。

## 数据库与备份

SQLite 数据库、WAL 文件和备份属于高敏感材料，必须按 API 密钥同等级别限制读取、传输和留存。数据库包含用于确定性重建公开 checkout token 的实例密钥，以及账务采集的原始响应和规范化交易事实；只保存 token 摘要是为了避免明文 token 出现在普通表查询、日志和导出中，并不能抵御已获得完整数据库或备份的攻击者。

POSIX 运行时会把文件创建掩码收紧到至少 `077`，并纠正数据目录和已识别敏感文件的 Unix mode；但拥有容器 root、Docker daemon、宿主机管理员权限或不受此 mode 约束的卷后端仍可读取数据。应用不会替部署者管理宿主机、卷后端或卷外备份的访问控制，必须分别限制 Docker 后端、部署文件和导出备份的访问。复制到卷外的备份不会由应用持续管理权限，复制完成后必须在目标存储重新施加访问控制。

数据卷剩余空间不足以保留运行余量和一份当前数据库副本时，readiness 与付款入口会失败关闭；备份也会在写入前检查目标文件系统。该检查存在固有 TOCTOU，不能替代主机磁盘告警、容量配额和独立备份监控。

在线备份只由 `backup` 的保留策略处理；异常备份文件只能按工具清单中的精确名称和 SHA-256 显式删除。迁移恢复点只由维护工具列出和恢复，不提供删除入口。恢复前隔离库会留在 `/data`，不会自动清理或由备份保留策略代管；只能用维护工具按清单保留最新数量，或按精确名称、SHA-256 和确认参数删除一份。所有清理都会拒绝任意路径、链接和执行期间发生的身份变化；不要用宿主机删除绕过锁、状态或完整性检查。拥有数据卷写权限的主体仍能绕过应用工具直接删除或替换文件，因此卷权限、异地副本和外部不可变保留策略仍是安全边界。

checkout 密钥版本、历史摘要与订单必须成套恢复。已有订单的数据库若丢失、篡改或错误改绑历史密钥，应用会拒绝启动，而不会生成新密钥掩盖损坏。备份卷保存的是明文 SQLite 副本；SHA-256、完整性检查和实例绑定只能发现部分损坏或错用，不能提供加密，也不能抵御拥有卷写权限的攻击者。完整恢复集必须包含选定备份及其精确 SHA-256、填写后的 Compose 或等价秘密配置、兼容的固定镜像引用和对应 Release 说明。默认两个卷位于同一主机，不是异地灾备；正式处理真实资金前必须建立并演练受访问控制的加密异地副本。完整流程见 [自动备份与恢复](docs/BACKUPS.md)。
