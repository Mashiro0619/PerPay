<!-- SPDX-License-Identifier: Apache-2.0 -->

# 安全策略

## 报告漏洞

请使用 GitHub Security Advisory 的私密报告功能提交漏洞。不要创建公开 Issue，也不要附带真实密钥、完整账务记录、回调地址或可识别个人信息；请使用最小化、脱敏的复现材料。

报告应尽量包含受影响版本、影响范围、复现条件和建议缓解措施。维护者会先确认收到，再完成影响评估、修复和协调披露。

## 支持范围

项目尚未发布稳定版本，当前代码不能用于处理真实资金。稳定发布后，本文件将列出受支持版本和安全补丁期限。

## 安全边界

- 不接受默认关闭 TLS、绕过响应验签、记录 secret 或放宽出站网络限制的变更。
- 示例数据必须为人工生成且明确标记为 fake。
- 依赖漏洞、密钥泄露和账务一致性问题按安全事件处理。
- 未经维护者协调，请勿对第三方平台或他人账户进行测试。

## 数据库与备份

SQLite 数据库和备份属于高敏感材料，必须按 API 密钥同等级别限制读取、传输和留存。数据库包含用于确定性重建公开 checkout token 的实例密钥；只保存 token 摘要是为了避免明文 token 出现在普通表查询、日志和导出中，并不能抵御已获得完整数据库或备份的攻击者。

实例密钥与历史 checkout 摘要必须成套恢复。已有订单的数据库若丢失或替换实例密钥，应用会拒绝启动，而不会生成新密钥继续运行。当前开发版本尚未提供实例密钥轮换和备份加密；正式处理真实资金前，这两项仍是发布门禁。
