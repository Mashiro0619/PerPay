name: perpay

x-perpay-host-port: &perpay-host-port "127.0.0.1:6190:6190"
x-perpay-public-url: &perpay-public-url "https://pay.example.com"
x-perpay-trusted-proxy-cidrs: &perpay-trusted-proxy-cidrs ""

services:
  app:
    ports:
      - *perpay-host-port
    environment:
      PERPAY_SECRETS_DIR: "/run/perpay-secrets"
      PERPAY_PUBLIC_URL: *perpay-public-url
      PERPAY_TRUSTED_PROXY_CIDRS: *perpay-trusted-proxy-cidrs
    image: ghcr.io/mashiro0619/perpay:latest
    volumes:
      - perpay-data:/data
      - perpay-backups:/backups:ro
      - perpay-secrets:/run/perpay-secrets
    user: "1000:1000"
    restart: unless-stopped
    stop_grace_period: 45s
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    init: true
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:6190/healthz').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      start_period: 5m
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  backup:
    image: ghcr.io/mashiro0619/perpay:latest
    environment:
      PERPAY_DATA_DIR: "/data"
      PERPAY_BACKUP_DIR: "/backups"
    volumes:
      - perpay-data:/data:ro
      - perpay-backups:/backups
    user: "1000:1000"
    command:
      - node
      - dist/backup/runner.js
      - schedule
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped
    stop_grace_period: 60s
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    init: true
    network_mode: none
    healthcheck:
      test:
        - CMD
        - node
        - dist/backup/runner.js
        - health
      interval: 5m
      timeout: 10s
      start_period: 10m
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  maintenance:
    image: ghcr.io/mashiro0619/perpay:latest
    profiles:
      - maintenance
    environment:
      PERPAY_DATA_DIR: "/data"
      PERPAY_BACKUP_DIR: "/backups"
    volumes:
      - perpay-data:/data
      - perpay-backups:/backups
    user: "1000:1000"
    entrypoint:
      - node
      - dist/backup/runner.js
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    init: true
    network_mode: none

volumes:
  perpay-data:
  perpay-backups:
  perpay-secrets:
