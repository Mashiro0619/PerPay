# 管理端验收记录

日期：2026-09-08（含 2026-09-07 的既有验收记录）。对象：管理端及关联的收款、安全边界；分阶段记录保留历史结果，汇总表以最近一次完整回归为准。本次审查修复、二维码上传及首次配置向导尚未提交、推送或发布。宿主环境为 Windows、Node.js 24.15.0，浏览器为 Chromium。

## 结论

本地自动化和浏览器验收通过，管理端使用真实 Hono 管理 API，并非静态样板。容器验收因本机 Docker Desktop 启动失败而未执行，远端 CI 也未运行；不能据此宣称本轮已经完成生产验收。

## 已执行

| 项目 | 结果 |
| --- | --- |
| `npm run check` | 退出码 0；版本、生成 API 一致性、前后端类型检查、前端构建、测试及服务端构建通过 |
| Node 测试 | 554 项通过，0 项失败，3 项 POSIX 文件权限测试在 Windows 上跳过 |
| 前端测试 | 17 个文件、245 项测试通过，包含首次配置向导、文字品牌与六字符密码策略回归 |
| 真实应用冒烟 | `test/admin-smoke.test.mjs` 使用真实 Hono 与隔离 SQLite，验证初始化、认证、深链接、分包资源、缓存、CSP、配置冲突、数据库重开后的持久化以及会话过期 |
| 初次浏览器页面矩阵 | 17 条页面路径 × 4 种尺寸 × 2 种主题，共 136 次检查通过；无页面横向溢出、未命名表单控件、内联样式或控制台错误 |
| 浏览器交互 | 12 组检查通过；另外实测了首次初始化、登录、零数据空态及原生刷新确认 |
| 源码质量检查 | `git diff --check` 与新增文件空白检查通过；本次七个向导相关 UI 目标的静态扫描返回空告警列表；早前动态图片属性误报保留在六项审查修复记录中 |

页面矩阵覆盖概览、订单列表及详情、待处理、账本与对账、冲突及异常详情、通知列表及详情、六个设置分类、运行状态与测试收款。尺寸为 1440×1000、834×1112、390×844、320×780；两种主题都启用了减少动态效果。

交互检查覆盖：主题键盘选择和焦点恢复、持久化与跨标签页同步、跟随系统变化、设置分类和侧栏离开保护、浏览器返回、真实 API 409 冲突保留草稿及请求编号、明确重新读取、保存后清除脏状态、退出登录清除受保护界面与会话、移动抽屉的背景滚动锁定和 Escape 焦点恢复。早期验收阶段仅持久化主题偏好；首次向导另增加按实例区分的标签页会话延后标记，不存储配置进度、表单草稿或密钥。

图表额外验证鼠标联动指示线、方向键及 Home/End、完整每日数据表，以及 7／30／90 天切换。金额和订单仍使用独立纵轴，不对不同单位做数值堆积。

原生刷新弹窗需要以 `agent-browser --no-auto-dialog` 验证；默认自动处理模式会干扰该检查。关闭自动处理后，确认出现 `beforeunload` 弹窗，取消刷新后草稿保持不变。存储不可用回退、图表边界数据与触摸事件处理由前端回归测试覆盖，不等同于真实手机硬件验收。

## 北京时间补充回归

时间显示和每日统计已统一为北京时间（Asia/Shanghai，UTC+8）。统计区间及订单、付款确认、业务通知的每日分桶都以北京时间零点为边界；数据库绝对时间不做整体偏移。

- `America/Los_Angeles` 进程时区下，8 项相关后端测试、20 项图表及格式化测试通过，覆盖北京时间零点、跨年、闰日、7／30／90 天区间的包含起点与排除终点，以及分桶总数与汇总一致性。
- 调整后重新运行完整 `npm run check`，退出码 0；上表测试数量已更新。管理员冒烟脚本也校验北京时间零点对应的 UTC 范围边界。
- 重新构建并重启隔离预览后，核对真实 API 范围和页面日期；概览在 1440px 浅色与 320px 深色下复测通过，无横向溢出或控制台错误。本次没有重复初次的全部 136 项页面矩阵。

## 首页与侧栏精简回归

- 删除首页收款链路步骤条，已完成配置的首页直接展示收款数据；未完成配置的引导和待处理事项保留，运行详情与实例版本仍在运行状态页。
- 删除侧栏实例名称、状态与版本区域，以及不再需要的状态轮询。底部主题、退出操作改为并排图标按钮，保留动态悬停提示、无障碍名称和 44×44px 点击区域；登录页的主题入口也使用同一图标按钮。
- 17 项相关导航／主题回归与前端类型检查通过。重新执行完整 `npm run check`，545 项 Node 测试、65 项前端测试通过，3 项 POSIX 权限测试按平台跳过。
- 浏览器确认桌面明暗主题、390px 移动抽屉和 320px 首页；两个图标按钮均为 44×44px 且同行，无文本或横向溢出。主题选择、Escape 关闭及焦点恢复、移动端真实退出登录正常；运行状态页仍能显示版本和各子系统详情，无页面或控制台错误。
- 已重新构建并重启隔离预览。此项是精简区域的定向检查，没有重复初次全部 136 项页面矩阵。

## 订单与表单打磨回归

- 保留北京时间、精简后的首页与侧栏，以及主题和退出的图标按钮；没有增加 UI 或 CSS 组件库。
- 降低基础表单选择器的优先级，消除订单搜索框的双重边框，统一查询和筛选控件的高度。手机输入保留 16px 字号，搜索框使用外层焦点边框，分页和详情入口保留 44px 点击区域。
- 订单列表按容器实际宽度适配：空间充足时保留表格，宽度不超过 780px 时逐条展示商品、金额、付款状态、收银台状态、北京时间与详情入口。表头和行列语义继续保留，概览中的精简订单列表复用同一实现。
- 筛选条件支持一键清除，空筛选结果可直接返回全部订单；清除时保留无关 URL 参数。后续空分页不再误报整个实例没有订单。修改查询内容会清除过期的查找错误，分页状态提供加载及页数播报。
- 新增 9 项回归测试，覆盖筛选清除、空态恢复、游标重置、查找纠错、长订单标识、表格语义及分页播报。最终 `npm run check` 退出码为 0：545 项 Node 测试与 74 项前端测试通过，3 项 POSIX 权限测试在 Windows 上跳过。
- 浏览器定向检查概览、订单、业务通知与经营码设置的 1440px／390px 明暗主题，共 16 组页面状态。最终订单列表另外覆盖 320、390、600、601、780、850、851、1080、1440px 九种宽度，页面与订单容器均无横向溢出；320px 下还检查了长商品名和长商户订单号的排版。
- 真实管理 API 验证了清除筛选保留查询参数、无结果恢复、上一页／下一页、错误查找后纠正商户订单号，以及内部编号查找。没有创建真实支付或发送业务通知。
- 订单页的桌面浅色与手机深色 axe 扫描未发现违规，键盘搜索焦点及手机表单字号另有实际样式检查。设置页扫描未发现违规，但横向滚动导航中被裁剪的项目有颜色对比度未决项；不把自动扫描等同于完整无障碍验收。主题切换后等待 CSS 过渡结束再扫描，避免把过渡中的颜色当作稳定状态。
- 本轮没有重复最初全部 136 项页面矩阵，也没有执行真实手机硬件、屏幕阅读器、Docker 或远端 CI 验收。

## 下拉菜单与焦点边框回归

- 原先 1px 控件边框外叠加了偏移 3px 的 3px 焦点轮廓，形成双层边框。下拉控件现使用与边框重合的 2px 内收焦点轮廓；键盘焦点仍清晰可见，没有移除其他控件的焦点提示。
- 六处原生下拉菜单共用 `appearance: base-select` 渐进增强样式，统一圆角、主题色、选中标记和选项间距；小屏选项至少 44px 高。没有更换表单元素、添加 JavaScript 选择器或引入组件库。不支持此 CSS 特性的浏览器保留原生菜单，本轮未使用旧浏览器验证回退外观。
- 管理端构建、前端类型检查和 74 项前端测试通过。实测方向键、Enter、Space、Escape、Tab、鼠标选择和真实订单状态筛选；六处控件展开时均为单层焦点边界。
- 订单查询及状态菜单检查桌面 1440px、窄屏 390px／320px 与明暗主题；选项未超出视口。移动端订单工具栏 axe 扫描未发现违规，保留原生标签和选择语义。本轮为样式定向回归，没有重新执行服务端全量测试、Docker 或远端 CI。

## 输入焦点、整行导航与登录精简回归

- 搜索框、普通输入框、密码框和多行输入统一使用与边框重合的 2px 内收焦点轮廓；搜索框内部输入不再单独绘制边框或焦点轮廓。保留键盘焦点提示，手机输入继续使用 16px 字号。
- 主题菜单移除可见的“外观主题”标题，保留单选组的无障碍名称及浅色、深色、跟随系统三个选项。登录页按截图删除左侧盾牌、说明、流程及左下角文字，删除登录说明与无需用户名提示；主标题、右下角署名、安全提示和初始化所需说明保留。
- 订单、业务通知，以及支付关联、账务异常、账本冲突三个对账列表，均移除独立操作列，支持点击行内非交互区域进入详情。记录标题保留真实链接，关联订单链接保持独立目标；键盘仍通过标题链接进入详情，不增加重复行焦点。
- 修复中断前回归中的 `MouseEvent` 构造异常，移除不必要的全局 `Window` 绑定。39 项定向测试与前端类型检查通过；重新执行完整 `npm run check`，545 项 Node 测试、90 项前端测试通过，3 项 POSIX 权限测试在 Windows 上跳过。
- 使用真实 Hono API 与临时合成数据，检查五类列表在 1440×1000、390×844 明暗主题下的 20 组状态：均可通过非链接单元格进入正确详情，无页面横向溢出或详情错误。手机通知和对账列表保留局部横向滚动；测试先滚动到可见单元格再点击，没有将视口外的自动化点击算作应用故障。
- 另行实测登录页删减、主题选项、密码／搜索／普通输入／多行输入的计算样式、整行键盘焦点及 Enter 导航，以及通知和支付关联中的独立订单链接。修饰键、中键和文字选择的事件分支由回归测试覆盖；不将低层浏览器工具未能可靠触发的中键操作视为真实浏览器验收通过。
- 六组定向 axe 扫描未发现违规；手机通知和对账表格仍有自动对比度判断未决项，不等同于完整无障碍验收。界面机械检查无发现，浏览器未记录页面错误。
- 本轮没有重复最初全部 136 项页面矩阵，没有执行真实手机硬件、屏幕阅读器、Docker、远端 CI 或真实支付／通知联调。没有提交或推送改动。

## 页脚精简与统一动效回归（2026-09-08）

- 删除全局页脚两段文字及相关布局样式；保留手机底部安全距离，不改变人民币金额和北京时间的业务口径。登录页未被本次页脚删除范围扩大影响。
- 新增原生动效体系，覆盖页面方向衔接、列表错峰、统计值显现、曲线描绘、分段选择指示器、主题颜色切换、弹出菜单、移动抽屉、弹窗进入、按钮与图标反馈。列表错峰最多延迟 120ms，最长图表描绘约 690ms，不使用金额跳数、循环装饰动画或新的动画依赖。
- 页面动画不靠路由重挂载实现，查询参数变化不重放整页动画；图表一旦收到指针或键盘交互就立即显示完整曲线。关闭弹窗、清除敏感数据及原有导航保护不等待动画结束。减少动态效果模式停止空间运动和曲线描绘，保留短颜色／透明度反馈；实测运行中切换偏好会立即取消正在播放的位移动画。
- 动态采样发现统计周期切换会先移除旧图表，造成布局收缩；已改为保留上一份数据并明确播报加载周期，待新数据成功返回再更新曲线。新增慢响应与快速切换回归，迟到的旧周期响应不会覆盖当前选择。
- 新增 11 项动效与周期切换回归，另补充页脚删除和图表交互断言。最终完整 `npm run check` 退出码 0：545 项 Node 测试、101 项前端测试通过，3 项 POSIX 权限测试在 Windows 上跳过，前后端类型检查与构建通过。
- 浏览器检查概览、订单、账本与对账、业务通知、经营码设置、待处理六类页面，覆盖 1440／834／390／320px 与明暗主题共 48 组状态：无页面横向溢出、内联样式、残留页脚或空闲后的非必要动画。定向实测图表描绘和中断、周期指示器、菜单选择与焦点恢复、移动抽屉快速开关及滚动锁、弹窗 Escape、搜索跳转和草稿取消保留，浏览器未记录页面错误。
- 周期修正后，在桌面浅色、手机深色及手机减少动态效果三种状态下，额外给统计请求注入 300ms 本地延迟：旧图表节点、统计区高度和图表起始位置保持不变，真实金额持续可见且加载标记正确复位；没有长任务或残留动画。Chromium 本地短采样不等同于真实手机性能或正式性能基准。
- 四组概览／订单定向 axe 扫描未发现违规；概览存在自动对比度判断未决项，不宣称完整无障碍验收。当前安装的 Impeccable 目录缺少机械扫描脚本，本轮该项未执行，没有沿用早前的无发现结果冒充当前扫描。
- 本轮没有重复最初全部 136 项矩阵，没有进行旧版浏览器、真实手机硬件、屏幕阅读器、Docker、远端 CI 或真实资金／通知验收。未提交或推送。

## 连续动效优化回归（2026-09-08）

- 针对“动画仍然生硬”的反馈，取消列表／数字的重复入场与图表周期切换后的整段描绘。普通页面改为原生新旧视图衔接，路由模块准备好后再切换；不支持原生视图过渡时保留可用的短淡入回退。
- 新增共享 SVG 选中指示器，覆盖侧栏、设置分类、统计周期和列表标签。快速切换从当前可见位置继续，不先跳回起点；响应尺寸变化重新定位。浏览器首轮发现窄屏设置分类的定位容器缺失，已修复，并在最终 48 组状态中复核指示器与选中项的几何对齐。
- 图表在周期变化后保留节点、可用的选中日期、展开的数据表与焦点；十字线和选中点平滑跟随，数值不做跳数或虚构插值。原生详情展开／收起、按钮按压、弹出菜单和移动抽屉统一节奏，无新增动画依赖或内联样式。
- 新增 11 项回归，覆盖指示器快速中断、减少动态效果、降级与卸载、私有路由不参与快照，以及图表日期／焦点／展开状态保留。最终完整 `npm run check` 退出码 0：545 项 Node 测试、112 项前端测试通过，3 项 POSIX 权限测试在 Windows 上跳过，前后端类型检查和构建通过。
- 最终浏览器确认概览、订单、待处理、账本与对账、经营码设置、运行状态六类页面，覆盖 1440／834／390／320px 和明暗主题共 48 组状态：无页面横向溢出、内联样式或选中指示器错位。另实测连续快速导航、图表光标、原生详情展开、抽屉中途反转和真实点击后的焦点恢复，并录制桌面与移动端动效。
- 在真实动画进行中切换减少动态效果，空间动画立即归零，后续导航不再启动页面快照。编辑配置后取消离开保留草稿；设置／测试收款／鉴权页面不参与页面快照；会话失效发生在视图动画中时，受保护内容立即移除且快照同步隐藏，不为退场保留敏感内容。
- Impeccable 当前可用的 launcher 对本次 20 个 UI 源文件执行一次批量机械检查，退出码 0、没有输出发现；浏览器未记录页面错误。此前记录中“工具缺失”的限制属于上一轮，不沿用到本轮。
- 本轮没有进行真实手机硬件性能基准、旧版浏览器实机、屏幕阅读器、完整无障碍审计、Docker、远端 CI 或真实资金／通知联调。原生动画的低能力回退由测试覆盖，不把本地 Chromium 结果等同于跨浏览器验收。没有提交或推送改动。

## 全站解释性文案精简回归（2026-09-08）

- 按“不是仅两处截图”的范围检查整个管理端：概览、订单与详情、待处理、对账与证据详情、通知与详情、六类设置、运行状态、测试收款及登录／初始化。移除重复的页头介绍、列表／页面脚注、数据来源声明、配置版本／更新时间说明及部分重复空态与面板解释；同时清除对应的失效样式。
- 设置保存按钮旁只在有实际修改时显示“有未保存的修改”，干净状态不再展示配置版本。后台 revision 请求参数、冲突报错、重新读取、离开确认和会话清理不变。备份说明简化为直接的“查看备份状态”入口。
- 保留字段格式、财务口径、错误与加载反馈；退款不转账、测试支付使用真实资金、密钥轮换使旧密钥失效等信息仍在对应操作中明确展示。理由必填与确认复选框、幂等重试和敏感明文自动清除不受影响。
- 新增 8 项回归，覆盖设置六分类不再显示版本脚注、未保存状态的出现／消失、退款登记警告；补充概览、实际列表、登录、空态和密钥弹窗断言。58 项针对性测试通过，最终完整 `npm run check` 退出码 0：545 项 Node 测试、120 项前端测试通过，3 项 POSIX 权限测试在 Windows 上跳过，类型检查及构建通过。
- 使用本地合成数据检查 1440px 浅色、390px 深色下的 15 类主页面／设置、5 类真实记录详情和登录／初始化，共 44 组展示状态；未发现页面横向溢出、内联样式、残留的页头／页脚冗余文案或页面错误。桌面及移动端截图均已检查，没有进行额外视觉改版。
- 实测编辑后未保存提示出现，取消离开保留输入，恢复原值后提示消失；打开密钥轮换、退款登记、测试收款确认关键风险仍在、确认限制未被绕过。没有实际执行密钥轮换、账务写入或付款。
- 针对本次 17 个 UI 源文件的一次 Impeccable 批量检查退出码 0，没有输出发现。浏览器仅使用隔离 Chromium；没有重复真实手机、旧版浏览器、屏幕阅读器、Docker、远端 CI 或支付／通知联调，未提交或推送。

## 经营码二维码图片识别回归（2026-09-08）

- 经营码内容框新增“上传二维码图片”入口，保留手动输入。PNG、JPG、WebP 在浏览器内解析，识别到的原始内容直接填入草稿，核对后通过原有 revision 配置接口保存；不上传图片、不跟随二维码链接、不宣称已验证收款方。
- jsQR 按需分包，本次构建产物约 130 kB（gzip 47.50 kB），不进入页面初始包；不依赖原生 BarcodeDetector，不放宽 CSP。限制 10 MB、2400 万像素和 8192px 源图边长，识别画布最大边长 2048px；读取后及时释放位图与画布，文件输入清空后支持重复选择。
- 新增 24 项解码测试与 11 项配置交互测试，覆盖真实 QR 像素、反色与 Unicode 内容、文件及像素限制、失败清理、重复选择、迟到结果、手动修改、分类卸载、保存等待、草稿确认和 revision 冲突。52 项定向测试通过，最终完整 `npm run check` 退出码 0：545 项 Node 测试、155 项前端测试通过，Windows 跳过 3 项 POSIX 权限测试，类型检查与构建通过。
- 在没有 BarcodeDetector 的隔离 Chromium 中实测普通 PNG、透明 PNG、旋转 JPG、反色 WebP 和含小二维码的海报，内容均原样填入；空白图、损坏文件、超限文件报错后原内容不变。真实 API 仅在点击保存后收到配置 PUT，重新读取保持识别结果；额外延迟真实图片解码，确认手动编辑会取消处理，迟到结果不会覆盖输入或自动保存。
- 桌面 1440px 浅色、手机 390px 深色及 320px 减少动态效果模式下检查上传、成功及错误状态，没有横向溢出或内联样式；键盘可聚焦按钮并触发文件输入。草稿取消离开保持内容，重新读取需要放弃确认。浏览器未记录页面错误或 CSP 违规，资源请求仅到本地同源。
- 两次定向 axe 检查均没有违规项，既有导航仍存在自动对比度判断未决项，不等同于完整无障碍验收。一次 Impeccable 批量检查退出码 0，没有输出发现。本轮没有执行真实手机硬件、旧版浏览器、屏幕阅读器、Docker、远端 CI 或真实支付／通知联调，未提交或推送。

## 六项审查修复回归（2026-09-08）

完整项目审查发现的六项可复现问题均已修复；未新增组件库或变更支付协议。续接任务时重新核对源码、测试和原始浏览器断言，保留原审查报告及截图，没有用后续结论覆盖历史复现证据。

| 问题 | 修复边界 | 回归覆盖 |
| --- | --- | --- |
| R1：跳过扫描被当作首次采集成功 | `SKIPPED` 保留先前状态、成功时间和失败信息，不建立或刷新采集成功时间；真正采集成功前不能开放收款 | 调度器回归，以及真实 RuntimeController、Hono 与隔离 SQLite：模拟支付宝请求为零时就绪和创建测试订单均返回 503；完成有效模拟采集后分别返回 200／201 |
| R2：并发初始化覆盖主密钥 | 临时密钥同步后以不可覆盖的硬链接原子发布，只接受目标已存在的竞争结果；清理本进程临时文件并重新验证最终密钥，异常文件拒绝启动 | 有效和无效竞争密钥均不会被覆盖；发布失败时清理自身临时文件并保留失败结果 |
| R3：过期或离线收银台仍引导付款 | 以服务端时间校准付款期限，考虑脚本加载、设备休眠及本机时钟偏差；到期移除普通／放大二维码来源和下载链接，关闭弹窗、隐藏付款引导，保留状态查询 | 8 项回归覆盖断网、请求失败、正负一小时客户端时差、延迟加载、休眠、有效期限恢复和迟到付款确认；客户端不会自行把未付款状态改写为已过期或已付款 |
| R4：临时会话查询失败丢失草稿 | 已登录会话遇到网络或 5xx 错误时保留编辑器和草稿，在原页面提供重试；明确的 401／403 仍清除受保护内容 | 保留原输入节点、450 秒草稿及离开确认，重试成功不重置；401／403 退出编辑器并清理配置缓存 |
| R5：标签页隐藏后迟到的密钥明文仍显示 | 从弹窗挂载时监听可见性，包括请求尚未完成的阶段；隐藏、卸载或超时后禁止再次显示明文 | 读取与轮换请求等待期间隐藏标签页，迟到响应均不展示明文；服务端已成功轮换时仍保留非明文配置更新，不自动重试轮换 |
| R6：退出后迟到保存响应恢复旧配置缓存 | 会话变化时中止旧请求，并对响应、响应体解析和 mutation 成功回调校验会话代次 | 旧请求与排队写入失效、旧 401 不影响新会话、迟到响应体和成功回调被拒绝、迟到配置保存不能回填缓存 |

### 自动化与浏览器结果

- 本次续接重新执行完整 `npm run check`，退出码 0：551 项 Node 测试、175 项前端测试通过，共 726 项通过；3 项 POSIX 权限测试在 Windows 上跳过。版本、生成 API 一致性、前后端类型检查和构建通过。相比审查时增加 26 项回归测试。
- 修复阶段的专用 Chromium 会话已实测桌面 1440px 浅色与手机 390px 深色：会话 503 后 450 秒草稿和未保存状态保留，取消离开及重试均不重置输入；两种尺寸均无页面横向溢出或内联样式。续接时核对了原始断言及桌面会话告警、手机过期收银台截图，没有把这次证据复核表述为重新运行全部页面矩阵。
- 使用模拟迟到响应，实测等待密钥读取期间隐藏标签页会关闭弹窗，返回后不出现明文；等待配置保存时触发会话失效，再投递旧成功响应并重新登录，页面重新读取配置并保持原值 300，没有恢复旧草稿或旧告警。没有实际读取真实密钥或写入配置。
- 在隔离预览中创建一笔合成测试订单，分别实测桌面和手机收银台。打开二维码放大窗口后缩短客户端期限并模拟网络失败，到期即清除两个二维码来源及下载链接、关闭弹窗并停止付款引导，保留 `UNPAID` 和状态查询；恢复请求并由服务端确认仍有效后，二维码可以恢复。未操作真实资金。
- 本次续接额外检查原浏览器会话的最终手机状态：订单已由服务端确认过期，无可见付款二维码、横向溢出、内联样式或页面错误。浏览器结果不等同于真实手机硬件或跨浏览器验收。
- 六个变更 UI 源文件的一次 Impeccable 静态扫描退出码 2，仅报告 `src/http/web/checkout.ts` 放大二维码的 `broken-image` 告警。已核对该未变更模板由 `imageSourceAttributes` 动态输出合法来源，非付款状态有意移除来源且关闭弹窗；这是静态解析误报，不是新增破图。没有添加忽略规则或改动模板来隐藏告警。工具的 context 命令未返回有效上下文，本次使用已有检测引擎并按现有实现复核，未修复或升级工具。
- 未重复最初全部 136 项页面矩阵，也未新增 axe、屏幕阅读器或真实手机验收。本次 Docker 探测仍无法连接 `dockerDesktopLinuxEngine`；容器、远端 CI、真实支付及通知联调未执行。没有提交或推送本次修复。

## 二维码拖拽上传与布局回归（2026-09-08）

- 按“上传优先”的结构调整：上方为整块可点击、始终显示虚线边框的上传卡片，居中显示“点击或拖拽二维码图片”；下方为独立的两行经营码编辑框，用于核对、粘贴或修改。拖入卡片时高亮并提示“松开即可识别”，没有给编辑框附加上传动作。
- 点击、Enter、空格均可打开文件选择；拖入图片复用原有本地 PNG／JPG／WebP 解码和大小限制，不自动保存。多文件或空文件拖入会取消旧识别并提示重试；保留手动编辑优先、异步结果隔离、保存期间禁用、卸载清理及页面外文件导航防护。
- 本轮 `npm run check` 退出码 0：551 项后端、197 项前端测试通过，3 项 Windows 不适用的权限测试跳过。其中上传组件 33 项测试覆盖新布局、点击／键盘／拖放、格式与大小校验、迟到响应和禁用边界。
- 使用最新生产构建在隔离 Chromium 中核对桌面 1440px 浅色和手机 390px 深色：上传卡片高 180px，编辑框约 64px，顺序与宽度正确，无页面横向溢出或新增内联样式。通过合成文件拖放事件驱动真实 PNG 解码；核对后显式保存仅发送一次带 revision 的配置 PUT。手机文件选择、重复识别及错误恢复通过，无额外保存、外部请求或页面错误。
- 本轮三个 UI 源文件的 Impeccable 检测返回空告警列表。浏览器使用合成二维码与隔离配置，不涉及真实支付或真实收款信息；未测试操作系统原生文件拖拽、真实手机硬件或跨浏览器矩阵。

## 首次收款配置向导回归（2026-09-08）

- 新增六步首次配置向导：准备应用密钥、配置支付宝接入、设置经营码、准备网站 API 密钥、可选通知与备份、检查收款就绪。未配置实例从首页进入，主动访问其他页面不强制跳转，已配置实例不因运行故障重新初始化。常规设置和管理员密码初始化入口保留。
- 必填步骤由服务端 `completion`／`next_step` 推导，URL 仅表示当前步骤；跳到未满足条件的步骤会返回首个缺失项。延后配置标记仅保存在当前标签页并按实例区分，取消离开不产生标记，退出登录时清除。
- 共用分类设置表单、revision 校验、二维码上传和密钥安全对话框。保存成功后解除对应表单草稿保护再前进；错误、冲突和临时会话查询失败保留草稿。通知与备份分别保存，最新 revision 随保存响应更新，另一项草稿不会被重置；顶部跳过入口不提交配置。
- 密钥生成后等待用户确认保存，不自动进入下一步；已有 API 密钥不自动轮换。明文隐藏、60 秒清理、迟到响应及离开后的保存回调均有回归覆盖。
- 最后一步每 5 秒检查真实系统状态，页面隐藏或离线时暂停；配置版本、收款版本与实例必须匹配，查询中或失败时不沿用旧绿色结果。允许收款的 `degraded` 状态仍保留警告；不会创建测试订单或发起支付。
- 最终 `npm run check` 退出码 0：551 项后端、237 项前端通过，3 项 Windows 不适用权限测试跳过；新增向导流程 26 项、就绪状态 14 项，原二维码与安全生命周期测试保持通过。
- 收尾时一次全量回归在既有 `test/database-startup-lease.test.ts` 的 150 毫秒租期断言失败，位置为停止 worker 后的到期时间检查。该文件及实现未修改；关闭测试预览后单独连续复跑三次通过，随后同一完整检查命令也通过。保留失败日志及复跑结果，未通过修改阈值、忽略测试或更改数据库代码隐藏问题；时序波动的具体原因尚未独立确认。
- 使用生产构建与隔离 SQLite，在桌面 1440px 浅色及手机 390px 深色验证管理员初始化、登录、向导跳转、生成应用密钥、配置保存、真实 PNG 本地解码、API 密钥保存确认，以及通知／备份草稿互不丢失。未启动支付宝或通知传输，所有配置与密钥为合成数据。
- 手机的步骤列表可展开，顶部“跳过，保留当前设置”无需滚动即可操作；大虚线上传卡片与两行编辑框保留。检查视口中无横向溢出、内联样式或页面错误。最终手机页正确停在等待首次采集，而不是错误显示配置完成即能收款。
- 另外以明确模拟的状态响应验证就绪成功及查询失败：允许完成后再返回 503，旧成功提示和完成入口立即撤销，没有创建测试订单。模拟结果不代表真实支付宝接入成功。官方菜单路径、真实支付、通知联调、屏幕阅读器、真实手机硬件和跨浏览器矩阵未在本轮验证。
- 首次向导的专用浏览器及预览进程已关闭，截图、检查日志和受测文件哈希保存在本次任务的本地验收目录。

## 认证页居中与文字品牌回归（2026-09-08）

- 登录／初始化页的“每一笔收款，都有据可查。”改为在左侧区域水平、垂直居中，保留原有两行文案与手机隐藏标语的布局。PerPay 名称旁的品牌图标在认证页、桌面侧栏和手机导航中移除，保留功能按钮图标及现有静态资源。
- 前端类型检查、生产构建、239 项前端测试及 `git diff --check` 通过；本轮仅修改前端，未重跑后端测试。四个变更 UI 目标的静态扫描返回空告警列表。
- Chromium 中检查 1440×960 深色和 960×768 浅色：标语区域相对左侧面板的水平／垂直中心偏差为 0px，两行文字分别居中；390×844 手机布局无横向溢出，无品牌图片或新增内联样式。
- 确认用户预览尚未初始化后，重新加载最新构建；配置向导预览继续运行在原地址，专用验证浏览器已关闭。

## 管理员密码最短长度回归（2026-09-08）

- 管理员首次设置和修改密码的最短长度统一从 12 降至 6 个 Unicode 字符，服务层、HTTP 校验、OpenAPI 契约、前端校验与提示一致；中文和 emoji 按字符而不是 UTF-16 单元数计数。
- 保留 1024 个 UTF-8 字节上限、无效 Unicode 拒绝、原有 scrypt 哈希、登录限流和改密后全部会话失效行为。现有密码不被修改，登录兼容性不增加限制；生产环境仍建议更长的随机密码。
- 新增及调整测试覆盖 5 字符拒绝、6 字符允许，ASCII／中文／emoji、首次设置与修改密码、旧会话注销及新密码重新登录。最终 `npm run check` 退出码 0：554 项后端、245 项前端通过，3 项 Windows 不适用权限测试跳过；API 类型一致性检查、构建与 `git diff --check` 通过。
- 确认用户预览仍未初始化后更新前后端，浏览器提示为“至少 6 个字符”，当前预览的 5 字符请求返回 422 且未创建管理员。允许 6 字符及改密流程通过隔离 HTTP 测试验证，没有替用户设置管理员密码。预览继续使用原地址。

## 支付宝教程与调用端压缩包（2026-09-08）

- 根据维护者提供的操作截图整理独立的 [支付宝经营码配置教程](../docs/alipay-setup.md)，从 README、USAGE 和前端开发说明链接进入；原 Obsidian 文件及截图未修改，也未把账户截图装入示例压缩包。
- 向导补充“支付宝搜索经营码并按提示申请”，区分应用公钥、支付宝公钥、网站 API 密钥和通知签名密钥；平台步骤按截图说明并提示以实际平台为准，不保证所有账户有账务明细查询权限。纠正“应用网关必须填写 PerPay 域名”的说法：当前实现主动查账，不提供该支付宝回调入口。
- 新增 Node.js 24 调用端示例，零第三方依赖，默认仅监听本机，密钥留在后端。示例支持创建／查询订单、相同业务单号与原参数重试、原始通知正文验签、毫秒时间戳检查、事件去重、订单版本保护和 SQLite 事务后 ACK；不执行真实履约操作。
- `npm run demo:package` 生成 `dist/demo.zip`，明确白名单共 12 个文件，可重复打包；包含运行说明、环境模板和测试，不包含实际 .env、数据库、日志、私钥、账户截图或依赖目录。使用 PowerShell Expand-Archive 在独立目录解压后，14 项示例测试全部通过，并直接用解压后的服务代码完成浏览器联调。
- 完整 check 流程退出码 0：576 项后端、247 项前端通过，3 项 Windows 权限相关测试跳过；包括新增 14 项示例测试、4 项真实路由／签名兼容测试、4 项打包测试和 2 项向导文案回归。API 类型一致性、前后端类型检查及构建通过。本机 PowerShell 的 npm 包装器首次未能启动检查，随后使用相同 npm 的 Node CLI 入口运行完整脚本链，保留启动错误和成功日志，不把空输出当作通过。
- Chromium 1440×960 浅色及 390×844 手机宽度下检查配置步骤、默认折叠说明、键盘焦点、示例必选确认与同单改价错误；向导另检查深色主题。界面无横向溢出，未出现页面脚本错误，静态设计扫描返回空列表。示例展示的是本机记录，地址配置不声称联网成功；未启用通知时明确要求主动查单。
- 浏览器联调用隔离 SQLite、合成密钥和显式合成的就绪状态；验证未勾选不创建、创建后查单、同单改价拒绝并保留输入，未启动支付宝和外部通知传输。HTTPS 通知公网可达性、支付宝真实账户权限、真实付款和生产履约仍需用户自行联调。
- 专用验收浏览器与 6294／6296 夹具均已关闭，用户原来的预览进程未重置；未提交或推送。

## 教程图文与易读性修正（2026-09-08）

- 后续精简：教程与 Demo 说明分别删减约 61% 和 63% 的非空白字符，保留全部 7 张图；22 项相关测试通过，压缩包已更新。本轮仅改文档，未重跑全量检查。
- 将原教程的 7 张截图完整复制到 `docs/assets/alipay-setup/`，按创建应用、打开设置、选择密钥、跳过生成工具、上传应用公钥、复制支付宝公钥和应用网关说明插入正文；保留原图的红框、打码及分辨率。
- 逐张查看截图，并用 SHA-256 确认仓库副本与来源完全一致，原 Obsidian 文档和 7 张图片均未改动。图片路径、正文链接和 Markdown 空白检查通过。
- 配置教程改为“打开哪里、点击什么、复制哪项、填到哪里”的操作说明；仍保留真实付款、查询权限和应用网关无需填写的事实边界。Demo 的上手步骤先讲运行和查单，签名、去重等开发细节移到末尾折叠区。
- Demo README 已同步重新打包到 `dist/demo.zip`，解压后验证说明文件与源码一致。示例、协议兼容与打包测试共 22 项通过，`git diff --check` 通过。本轮仅修改文档和图片，没有重跑完整前后端检查，也没有改动运行中的预览。

## 收银台界面重做（2026-09-09）

- 改为最大 480px 的单列付款页：状态与倒计时、单一主金额、二维码、操作、订单明细。移除左右分栏、大块留白、装饰图标和重复提示；沿用 PerPay 深浅色与系统字体，不增加依赖。
- 放大窗口显示同一应付金额；成功、争议、关闭、过期和服务异常保持真实状态，继续保留二维码清理、下载失效、焦点转移与防重复查询。状态更新集中到无障碍播报，不再增加第二个结果面板。
- 修复二维码加载失败被定时查单错误清除的问题；错误和禁用的二维码操作持续保留，单独重载图片后恢复。服务异常时隐藏倒计时，避免继续显示付款期限。
- 完整 `npm run check` 通过：576 项后端、255 项前端测试通过，3 项 Windows 不适用权限测试跳过。收银台 DOM 回归增至 16 项，覆盖主界面与放大金额同步、终态、503 恢复及图片失败；差异检查通过。
- Chromium 检查 1440×960 浅色、390×844 深色及 320px 长商品／长单号／最大金额，无横向溢出。检查正常、成功含退款、过期、503、404、放大和 Escape 返回焦点；实际拦截图片请求验证失败后查单不清错、重载恢复。桌面和手机 axe WCAG 2 A/AA 扫描均为 0 违规，无页面脚本错误。
- 静态扫描仅报告放大二维码模板的 `broken-image`：来源由 `imageSourceAttributes` 动态生成，不能按静态 HTML 判断。浏览器中普通和放大二维码图片均加载成功（固有尺寸 150×150）；终态有意移除来源并关闭弹窗，未添加忽略规则。
- 所有浏览器数据来自临时 SQLite、合成密钥与模拟就绪状态，不连接支付宝或外部通知。原预览链接可跳转到最新测试订单；专用验收浏览器已关闭，预览服务保留供查看。未验证真实付款、跨浏览器或真实手机硬件，未提交或推送。

## 用户视角审查修复（2026-09-09）

- 首页对已配置实例也检查收款状态；展示停收原因或非阻断告警。每 30 秒更新，隐藏时暂停，离线和查询失败不沿用旧的正常结论；运行故障不会触发初始化。
- 前后端共用经营码格式检查，仅接受原始 `qr.alipay.com` HTTP/HTTPS 链接。旧的无效内容保留供修改，但不能通过就绪检查。格式检查不验证收款方身份。
- 无效支付宝公钥、私钥和经营码显示字段错误并聚焦，保存失败保留草稿。订单、通知、待处理与对账列表用 URL 和历史记录保留分页；详情返回保留原筛选，清除筛选回到首页。
- 增加停服后的管理员密码恢复命令；不开放匿名重设接口。覆盖停服检查、维护锁、缺失／未初始化／版本不符数据库、密码限制、旧会话注销、订单与密钥保留及审计记录。
- 二维码改存 PNG；覆盖重复点击、生成失败、过期、付款完成、隐藏和卸载清理。Chromium 实际下载 1050×1050 PNG，并通过上传组件回读出原合成经营码；未连接支付宝或发送业务通知。
- 桌面 1440px 浅色和手机 390px 深色检查首页告警、字段错误、草稿、分页返回与收银台，无横向溢出。静态扫描仅报二维码动态模板的既有 `broken-image` 误报，实际图片加载与下载成功；未添加忽略规则。
- 最终 `npm run check` 退出码 0：后端 603 项、前端 275 项通过，3 项 Windows 不适用权限测试跳过。一次全量运行中既有租约测试出现 `database is locked`，单测复跑及关闭隔离预览后的完整检查通过；未修改该测试或租约逻辑。
- Docker Linux 引擎未运行，未验证容器启动、证书签发、真实手机支付宝或真实付款。专用浏览器和隔离预览已关闭；临时文件删除受本机策略限制，合成数据库留在系统临时目录。未提交或推送。

## 调用端 Demo 默认回调（2026-09-09）

- 通知地址和签名密钥改为启动必填，新订单自动带 `notify_url`；漏配不会静默退回查单模式。旧订单仍按原参数重试，没有回调地址时明确提示手动补查。
- 页面每 5 秒读取本地回调结果，不自动查 PerPay；保留“手动查单”作补偿。原始正文验签、通知去重、版本保护和持久化后 ACK 继续通过回归。
- 完整 `npm run check` 退出码 0：605 项后端、278 项前端通过，3 项 Windows 平台测试跳过。Demo 相关 24 项接口／打包测试与 3 项页面测试通过；重新生成 `dist/demo.zip`，解压独立执行 16 项测试通过，不包含真实密钥或数据库。
- 隔离 Chromium 桌面 1440px、手机 390px 验证签名回调后自动显示付款确认，补查次数为 0，无页面错误或横向溢出，静态扫描无发现。使用内存数据库和合成通知，未进行真实付款或公网回调联调；验收服务已关闭，未提交或推送。

## 未执行与发布前门槛

- Docker Desktop 启动后因内部 `dockerInference` socket 文件访问错误退出，Linux 引擎不可用。没有构建或运行本轮容器，未用远端 `latest` 代替当前源码成果。
- 当前源码镜像构建、独立 Compose 临时卷、容器内初始化／配置保存／重启持久化、镜像中不含演示程序和固定测试密码等运行时检查，仍需在 Docker 恢复后执行。
- `scripts/admin-smoke.mjs` 已接入 Container validation 与 Release 工作流，但接入不代表这些工作流已通过。远端 CI、镜像发布和真实支付联调均未执行。
- 正式发布前必须从当前源码构建镜像，按管理前端 README 的隔离流程运行 `initialize` 和重启后的 `verify`，并取得对应远端 CI 结果；不得在实际收款实例上运行验收初始化脚本。

## 本地预览边界

隔离预览地址为 `http://127.0.0.1:6194/admin`；未初始化实例位于 `http://127.0.0.1:6195/admin`。本次专用预览已在验收后关闭，启动方式和演示账户见管理前端 README。

预览使用临时 SQLite 与合成业务记录，不读取生产数据库，也不启动支付宝或通知网络传输。验收没有操作真实资金或发送真实业务通知。交付前重启隔离预览会重置演示账户配置和业务数据；已有浏览器会话需重新登录。
